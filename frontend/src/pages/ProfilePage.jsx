import PagePlaceholder from './PagePlaceholder.jsx';

export default function ProfilePage() {
  return (
    <PagePlaceholder
      eyebrow="Profile"
      title="Patient profile shell is ready."
      description="The route and layout are ready for personal details, emergency contact and validation handling from user-service."
    />
  );
}

