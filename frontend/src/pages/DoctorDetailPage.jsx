import PagePlaceholder from './PagePlaceholder.jsx';

export default function DoctorDetailPage() {
  return (
    <PagePlaceholder
      eyebrow="Doctor Detail"
      title="Doctor profile and slot preview scaffold."
      description="Phase 2 can connect this view to timeslot availability, public profile data and appointment CTA logic without reshaping the route tree."
    />
  );
}

