import PagePlaceholder from './PagePlaceholder.jsx';

export default function NotificationPage() {
  return (
    <PagePlaceholder
      eyebrow="Notifications"
      title="Notification inbox scaffolded for appointment events."
      description="Phase 3 can bind this page to notification-service without changing the protected navigation or session plumbing."
    />
  );
}

