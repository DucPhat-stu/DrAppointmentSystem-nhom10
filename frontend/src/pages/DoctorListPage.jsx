import PagePlaceholder from './PagePlaceholder.jsx';

export default function DoctorListPage() {
  return (
    <PagePlaceholder
      eyebrow="Doctors"
      title="Doctor directory route is ready."
      description="Use this page to wire search, specialty filters and doctor cards once the user-service contract is implemented in Phase 1."
    />
  );
}

