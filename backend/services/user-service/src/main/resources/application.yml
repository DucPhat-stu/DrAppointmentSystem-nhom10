server:
  port: ${APP_PORT:8082}

spring:
  application:
    name: user-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/user_db}
    username: ${DB_USERNAME:healthcare}
    password: ${DB_PASSWORD:healthcare}
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  flyway:
    enabled: true
  rabbitmq:
    addresses: ${RABBITMQ_URL:amqp://healthcare:healthcare@localhost:5673}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

logging:
  pattern:
    level: "%5p [traceId:%X{traceId:-},userId:%X{userId:-},role:%X{role:-}]"

app:
  service:
    name: user-service
    description: Patient profiles and doctor directory foundation.
    database: user_db
    rabbitmq-enabled: false
    redis-enabled: false
  internal:
    service-token: ${INTERNAL_SERVICE_TOKEN:change-this-before-shared-environments}

security:
  jwt:
    secret: ${JWT_SECRET:change-this-before-shared-environments}
